{
  "name": "homestead-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Holds the Anthropic API key so the mobile app never has to, and runs the contractor dispatch service.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
